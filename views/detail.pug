doctype html
html
    head
        title= board.title
        meta(charset="utf-8")
        style.
            body {
                margin: 0;
                padding: 0;
                background: linear-gradient(135deg, #2575fc, #6a11cb);
                font-family: Arial, sans-serif;
                display: flex;
                justify-content: center;
                align-items: center;
                height: 100vh;
                color: white;
                animation: fadeIn 1.5s ease-in-out;
            }

            .container {
                text-align: center;
                max-width: 600px;
                padding: 30px;
                background: rgba(255, 255, 255, 0.15);
                border-radius: 15px;
                backdrop-filter: blur(10px);
                box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
                animation: slideUp 0.5s ease-in-out;
            }

            h1 {
                font-size: 2rem;
                margin-bottom: 10px;
                font-weight: bold;
            }

            .content-box {
                font-size: 1.2rem;
                background: rgba(255, 255, 255, 0.2);
                padding: 20px;
                border-radius: 10px;
                margin-top: 15px;
                text-align: left;
                line-height: 1.6;
            }

            .back-btn, .edit-btn, .delete-btn {
                display: inline-block;
                margin-top: 20px;
                padding: 10px 20px;
                font-size: 1.2rem;
                color: white;
                background: rgba(255, 255, 255, 0.2);
                border: 2px solid white;
                border-radius: 30px;
                text-decoration: none;
                transition: all 0.3s ease;
            }

            .back-btn:hover, .edit-btn:hover, .delete-btn:hover {
                background: white;
                color: #2575fc;
                transform: scale(1.1);
            }

            .edit-btn {
                background: rgba(255, 255, 0, 0.3); /* Yellow for edit */
                margin-right: 10px;
            }

            .delete-btn {
                background: rgba(255, 0, 0, 0.3); /* Red for delete */
            }

            @keyframes fadeIn {
                from {
                    opacity: 0;
                }
                to {
                    opacity: 1;
                }
            }

            @keyframes slideUp {
                from {
                    opacity: 0;
                    transform: translateY(20px);
                }
                to {
                    opacity: 1;
                    transform: translateY(0);
                }
            }
    body
        .container
            h1= board.title
            .content-box
                p= board.content
            a.back-btn(href="/boards/list") 목록으로 돌아가기
            a.edit-btn(href=`/boards/edit/${board.id}`) 수정하기
            a.delete-btn(href=`/boards/delete/${board.id}`) 삭제하기
